name: Update Codex Desktop

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: nixslop-update-global
  queue: max
  cancel-in-progress: false

jobs:
  update:
    name: Run Codex Desktop update
    uses: ./.github/workflows/update-reusable.yml
    with:
      program: codex-desktop
    secrets:
      CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
